// #!omni:code --rust
// [BLOCK:ROOT] THE BODY: THE GAME ENGINE (NATIVE CLI)
// ==============================================================================

pub mod primitives;
pub mod kernel;
pub mod interface;

// The native logic is held in the internal modules.
// src/main.rs handles the entrypoint.
